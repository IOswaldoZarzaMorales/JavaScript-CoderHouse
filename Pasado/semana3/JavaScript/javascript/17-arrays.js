const producto1 = 'harina';
const producto2 = 'aceite';
const producto3 = 'azucar';

//Array

const productos = ['harina', 'acetite', 'azucar'];

console.log(productos[0]); //Acceder a una posicion especifica
console.table(productos)//Muestra en formato de tabla el array

console.log(productos.length);//Saber el numero de elementos

for (let i = 0; i < productos.length; i++){//For para recorrer arrays
    console.log(productos[i]);
}

