//?Notacion

// ;  ----> Delimitar el final de una linea
const b = 4;
b + 4;
// . ---->Se utiliza en los objetos para acceder a los atributos
// movil.anchura

// []---->Se utiliza para listas, arreglos o arrays
const ar = [1,2,3,4]
console.log(ar[2]);

// ()--->Parentesis se usan para funciones
function suma  (a,b){

}

//{} LLaves para objetos, funciones y estructuras de control
const movil ={
    anchura : 5,
    altura: 10
}

if (true){
    //Todo lo que haya entre llaves 
    const constante2 = 'Hola';
}

