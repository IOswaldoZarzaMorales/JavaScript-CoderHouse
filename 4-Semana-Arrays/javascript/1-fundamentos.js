//?<---------  ------->

const productos = ['harina', 'manteca', 'azucar'];

console.log(productos[0]);//?Acceder a un elemto en especifico

console.table(productos);//?Nos muestra el arrray en forma de tabla

console.log(productos.length);//?Nos da la longitud de nuestro array

for (let i = 0; i < productos.length ; i++){ //?Ciclo para recorrer los elementos del array
    console.log(productos[i]);
}

console.log(productos);